{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "pwa-chrome",
            "request": "launch",
            "name": "Run AVR SCADA Dashboard",
            "url": "http://localhost:8080/avr_scada_full_demo.html",
            "webRoot": "${workspaceFolder}",
            "preLaunchTask": "Start AVR web server"
        }
    ]
}
